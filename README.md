# Axum Example

This repo is mentioned on this [post](https://marccamara.com/posts/1)

You can run this exampple by running:

```
cargo run
```

and then accessing `localhost:3000` or `localhost:3000/up` in your browser.
